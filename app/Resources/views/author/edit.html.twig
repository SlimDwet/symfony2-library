{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="page-header">Edition d'un auteur</h1>

    <div>
        <a href="{{ path('author_index') }}" class="btn btn-primary">Liste des auteurs</a>
        {{ form_start(delete_form, { 'attr': { 'style': 'display: inline' } }) }}
        <input type="submit" value="Supprimer" class="btn btn-danger">
        {{ form_end(delete_form) }}
    </div>
    <br>

    {{ form_start(edit_form, {'attr': { 'class': 'form-horizontal' }}) }}

    <div class="form-group">
        {{ form_label(edit_form.firstname, 'Prénom', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-4">
            {{ form_row(edit_form.firstname, {'attr': {'class': 'form-control'}, 'label': false}) }}
        </div>
        {{ form_errors(edit_form.firstname) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.lastname, 'Nom', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-4">
            {{ form_row(edit_form.lastname, {'attr': {'class': 'form-control'}, 'label': false}) }}
        </div>
        {{ form_errors(edit_form.lastname) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.nickname, "Nom d'artiste", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-4">
            {{ form_row(edit_form.nickname, {'attr': {'class': 'form-control'}, 'label': false}) }}
        </div>
        {{ form_errors(edit_form.nickname) }}
    </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-4">
            <input type="submit" value="Modifier" class="btn btn-default" />
        </div>
    </div>
    {{ form_end(edit_form) }}

{% endblock %}
